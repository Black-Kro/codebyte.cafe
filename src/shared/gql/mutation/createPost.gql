mutation createPost($content: String!) {
    createPost(input: { content: $content }) {
        author {
            userId
            username
            profile {
                displayName
                bio
                
            }
        }
    }
}